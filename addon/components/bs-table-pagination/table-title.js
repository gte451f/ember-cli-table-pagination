import Ember from 'ember';
import layout from '../../templates/components/bs-table-pagination/table-title';

export default Ember.Component.extend({
  layout,
  classNames: ['box-header', 'with-border']
});
