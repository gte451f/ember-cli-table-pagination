<tbody>
{{#each record in this}}
    <tr>
        {{#each col in columns}}
            <td>{{pager-loop record col}}</td>
        {{/each}}
        <td class="clearfix">
            {{#link-to linkPath record class="btn btn-primary btn-xs pull-right"}} 
                {{fa-icon 'folder-open'}} 
            {{/link-to}}
        </td>
    </tr>
{{else}}
    <tr>no records found ... :-(</tr>
{{/each}}
</tbody>
