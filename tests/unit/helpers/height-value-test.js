import { heightValue } from 'dummy/helpers/height-value';
import { module, test } from 'qunit';

module('Unit | Helper | height value');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = heightValue([42]);
  assert.ok(result);
});
